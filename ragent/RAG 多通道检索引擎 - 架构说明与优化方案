检索架构分析与优化方案
在阅读代码过程中，发现现有检索架构的一些优缺点，以下是我的思考，仅供大家参考。
基于意图路由的分层检索架构，支持意图定向检索 + 全局向量检索，采用多通道并行、线程池隔离、插件化处理器设计，兼顾检索效率与系统稳定性。
📋 一、当前架构说明
- 最小调度单元：子问题（SubQuestion）
- 上下文模型：全局 SearchContext（打包子问题下所有意图）
- 检索策略：按子问题最高意图分数统一决策
        
  - 高分 → 意图定向检索
  - 低分 / 无意图 → 全局向量检索
- 执行模型：多通道并行检索 + 链式后置处理
- 线程模型：业务线程池隔离（RAG 上下文 / MCP / 检索 / 意图识别）
✅ 二、现有架构优点
1. 架构简洁、维护成本低
以子问题为核心检索单元，统一封装上下文、通道调度、后置处理，逻辑清晰、业务边界明确，便于迭代与维护。
2. 资源开销可控、并发稳定
按子问题做异步并行，避免细粒度意图并行带来的任务爆炸、线程池耗尽问题，线上稳定性更强。
3. 检索策略收敛、容错性高
以单个子问题的最高意图分数做全局路由，策略统一、决策简单，避免多规则冲突，适合生产环境。
4. 多通道并行、检索时延低
向量库、知识库、全文检索等通道并行执行，整体耗时由最慢通道决定，显著降低 RAG 召回阶段耗时。
5. 插件化设计、扩展性强
检索通道、结果处理器均基于接口扩展，支持灵活增删数据源、重排规则、过滤策略，符合开闭原则。
6. 可观测性完善
全链路追踪、通道执行日志、耗时统计、结果数量监控，结合 TTL 上下文透传，便于线上问题定位。
❌ 三、现有架构缺点
1. 检索粒度粗，意图利用率不足
同一子问题下多个意图共用一套检索策略，高分意图与低分意图无法差异化路由，弱意图无法触发兜底检索。
2. 决策逻辑简单，召回上限受限
仅依靠最高分做通道判断，忽略中低置信度意图的潜在价值，复杂问题易出现召回不足。
3. 通道互斥，不支持混合召回
意图定向通道与全局向量通道互斥，无法在同一子问题内同时执行精准召回 + 泛化召回，覆盖度不足。
4. 上下文捆绑重，隔离性差
多个意图共享同一个 SearchContext，通道无法按意图过滤，难以实现定制化检索规则。
5. 结果分组粗糙，溯源能力弱
所有检索块（Chunk）简单复制给所有意图，无法实现 意图 ↔ 文档精准绑定，影响上下文质量与可解释性。
🔧 四、架构优化方向（生产可落地）
1. 检索粒度优化：子问题级 → 意图级并行
将调度单元从 SubQuestionIntent 改为单个 NodeScore，每个意图独立构建上下文、独立路由通道。
        
效果：高分走定向、低分走全局，同一子问题支持混合策略
2. 路由策略升级：分层阈值路由
高置信度（≥阈值）：意图定向检索；中置信度（区间）：双通道混合检索；低置信度（＜阈值）：全局向量检索。
        
效果：兼顾精准度与覆盖度，解决通道互斥问题
3. 上下文结构优化：支持意图隔离
SearchContext 支持单意图透传、多意图隔离标记，通道 isEnabled 增加意图筛选能力。
        
效果：减少无效检索，支持细粒度业务规则
4. 检索结果精细化：意图 - Chunk 精准绑定
按意图标签、知识库、文档来源做精准映射，替代全量 Chunk 复制逻辑。
        
效果：降低上下文冗余，提升 LLM 回答质量
5. 通道高可用：熔断、降级、限流
慢通道超时控制、异常熔断，单通道异常不影响整体流程。
        
效果：提升系统稳定性
6. 动态调度：多级 Rerank + 自适应 TopK
按意图分数动态调整召回条数，增加轻量重排、去重、过滤。
        
效果：提升检索精度，降低 Token 开销
7. 线程与缓存优化
核心通道使用高优线程池，兜底通道使用低优队列，高频意图结果缓存。
        
效果：降低向量库 / DB 压力，提升吞吐量
📌 五、适用场景
适合当前架构
- 中低复杂度业务问答
- 意图体系清晰、领域边界明确
- 追求稳定性、低时延、易维护
适合优化后架构
- 多领域、泛领域问答
- 复杂问题、多意图并存
- 高要求召回覆盖与回答准确率
📖 六、文档说明
本文档用于：
- 架构评审与技术方案
- 面试表述与项目介绍
- 系统迭代与重构依据
- 团队协作与新人上手
